export const metadata = {
  title: "Shop | Cozy with Anne",
  description: "Shop prints, stickers, and original art by Anne.",
};

export default function ShopLayout({ children }) {
  return children;
}
